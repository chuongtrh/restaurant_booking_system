{
  "name": "restaurant_booking",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "clean": "rimraf node_modules",
    "dev": "nodemon app.js",
    "start": "node app.js",
    "db:migrate": "sequelize-cli db:migrate --env local --config ./config/database.config.js",
    "db:migrate:undo": "sequelize-cli db:migrate:undo --env local --config ./config/database.config.js",
    "db:migrate:undo:all": "sequelize-cli db:migrate:undo:all --env local --config ./config/database.config.js",
    "db:seed": "sequelize-cli db:seed:all --env local --config ./config/database.config.js",
    "db:seed:undo:all": "sequelize-cli db:seed:undo:all --env local --config ./config/database.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chuongtrh@gmail.com",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "moment": "^2.29.1",
    "pg": "^8.4.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.3.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "sequelize-cli": "^6.2.0"
  }
}
